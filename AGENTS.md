
## GitHub 提交流程

本项目推送到 GitHub 时，使用 Astar 已开启的本地 HTTP 代理；不要改写全局 Git 代理配置。每次推送仅对当前命令临时指定代理：

```powershell
git -c http.proxy=http://127.0.0.1:58309 push origin main
```

推送前先确认本地提交与远程可访问；推送后用同一代理核对远程 `main` 的提交 SHA。若 `127.0.0.1:58309` 不可用，应报告网络链路问题，不得猜测或写入其他代理地址。

## Mira Agent 最高优先级约束：逐步可见的信息路径

以下约束优先于任何为了简化实现而设计的 Agent 工作流。修改 Agent、工具、鼠标动画、页面观察或任务 Runtime 前，必须先用这些约束审查方案。

### 1. UI 工具是人能完成的界面操作

凡是通过现有页面完成的工作，统一由三类通用能力组合完成：`page.inspect`（只读取当前可见页面语义）、页面组件注册表（声明真实组件和允许的鼠标动作）、通用鼠标动作（移动、点击、双击、长按、悬停、拖拽、滚动、输入、按键）。

不得为了剧本、目录、分集等某个页面对象另造 `script.directory.read`、`script.episode.read`、`script.search` 一类会直接返回页面数据的 UI 业务工具；目录条目、正文、滚动容器本身就是被观察和被操作的页面组件。这样 Agent 能做的严格等于用户在界面中能做的。非 UI 的专家知识或外部服务可以另设工具，但不得绕过页面可见性读取当前项目内容。

### 2. Agent 不得预知未来步骤的信息

一个任务开始时，Agent 只能知道用户请求和已获得的上下文。它不得预先获得完整目录、总集数、正文、搜索结果或任何尚未通过当前步骤抵达的信息。

每一次工具调用只可返回该工具在人类操作路径的**当前阶段**可以看到的结果。例如：

```text
打开剧本 → 只得到“剧本已打开、目录按钮可用”
打开目录 → 只得到当前可见的目录条目与滚动状态
滚动目录 → 只得到滚动后的可见条目与新的滚动状态
```

不得用“后台已有数据”提前填充后续步骤的结果。后台实现可以可靠地驱动页面状态，但不能向 Agent 泄露尚未到达的页面事实。

### 3. 每一步都必须形成完整的 ReAct 边界

```text
Agent 决策 → 调用页面观察或通用鼠标动作 → 鼠标执行对应动作 → 页面状态变化
→ 返回该步可见事实 → 结果进入下一轮上下文 → Agent 再决策
```

同一任务的下一轮决策必须等待当前工具处于 `succeeded`、`failed` 或 `cancelled`。工具结果不能插入已经发出的模型请求，也不能由 Runtime 根据任务标题补写下一步。

### 4. 操作知识、页面事实与业务结论必须分离

- `guide.lookup` 只回答“通常如何通过界面完成操作”，不提供当前项目答案。
- 页面观察只回答“此刻页面中实际可见什么”。
- 最终回答只能基于任务已获得的证据；“最后编号是第 N 集”不自动等于“共有 N 集”。遇到第 0 集、跳号、重复项时必须继续核对或明确说明证据边界。

### 5. 鼠标表演必须与工具语义一致

当 Agent 要查看目录、打开分集或阅读正文时，必须先依据页面观察选择真实组件和通用鼠标动作；鼠标必须表现出对应的移动、点击、悬停或滚动。滚动必须尊重方向、距离和容器，不能无论参数为何都跳到固定位置。

### 6. 禁止固定任务剧本

Runtime 负责状态机、工具状态、动作排队、页面变化和下一轮唤醒；它不负责决定“应打开剧本、打开目录或滚到末尾”。这些选择必须由唯一 Agent 根据当前轮的操作知识与工具结果自行做出。

完整设计与验收场景见 `docs/agent/03-human-path-execution-contract.md`。如实现与该文档冲突，应先更新文档并明确说明原因，再修改实现。

### 7. 页面观察与空间定位必须严格分层

`page.inspect` 是 Agent 获得页面语义信息的唯一来源：当前显示的文本、当前可见目录条目、是否在顶部/底部、可见按钮与页面状态都只能由它进入 Agent 上下文。

组件位置解析层只为鼠标动作系统提供位置数据，例如目录中心、滚动条轨道/滑块、当前可见条目的中心点。它不得向 Agent 返回目录内容、隐藏条目、总数、正文或搜索结论。

当 Agent 根据 `page.inspect` 决定“滚动目录”时，Runtime 才向组件位置解析层请求目录中心，然后驱动鼠标移动、悬停、滚动。动作完成后必须再次 `page.inspect`，新内容才可进入下一轮。若某集尚未由页面观察显示，位置解析层不得给出它的隐藏屏幕坐标。

### 8. Agent 方案与实现前的必检清单

每次思考、设计、实现或修改 Mira 相关内容前，必须逐项确认：

1. Agent 本轮获得的页面语义信息是否全部来自 `page.inspect`？
2. 位置工具是否只把坐标交给鼠标/动作系统，而未泄露业务内容？
3. 每个动作完成后是否会产生新的页面观察，再唤醒下一轮？
4. 是否让 Runtime 或工具替 Agent 决定了下一步？如有，必须移回 Agent 决策。

任一项不成立，不得继续实现或宣称方案符合 Mira 架构。

## 🚀 性能优化圣经：意图驱动懒恢复 (Intent-Driven Lazy Restoration)

在处理类似“无限画布（Infinite Canvas）”这种包含大量 DOM 节点和重度 CSS 特效（如 `box-shadow`, `backdrop-filter`）的场景时，GPU 的栅格化（Rasterization）和过渡动画（Transition）往往是导致缩放和拖拽卡顿的根本原因。

传统的优化方案是在操作时剥离特效，操作结束后（通常是 150ms 左右的短定时器）立即恢复。但这在用户进行高频的“缩放 - 拖拽 - 缩放”交替操作时，会导致极其严重的“微卡顿”（因为短定时器会在移动途中意外触发，引发极其昂贵的同步重排 Synchronous Reflow）。

**这是由用户 @mrluetang 在 2026 年提出并验证的一种极具交互心理学洞察的高级架构思想：**

放弃基于“短时间死板倒计时”的恢复策略，转而利用**“用户的实际交互意图”**作为恢复高耗能样式的触发器。

### 核心机制

1.  **持久降级 (Persistent Degradation)**：一旦检测到滚轮（缩放）或中键（拖拽），立刻关闭所有昂贵的 CSS 特效。**不要急于恢复**。让用户在轻量化的状态下尽情地连续移动。
2.  **一级意图触发器 (Instant Restoration)**：利用“注意力转移”的心理学现象。当用户执行以下明确表明“我要停下来操作当前视野内的东西了”的动作时，立即（以 0 毫秒过渡时间，即时干贴）恢复所有特效：
    *   点击鼠标左键（选中卡片、点击按钮、准备输入）。
    *   打开全屏/半屏覆盖层（如侧边栏、设置面板）。
    *   *此时用户的视觉焦点在具体的操作目标上，几乎感知不到背景瞬间恢复特效带来的细微帧率波动。*
3.  **终极托底 (Absolute Idle Fallback)**：用一个极长（例如 2000ms）的“绝对静止计时器”作为最后的安全网。只有当用户长达 2 秒没有任何滚轮和中键操作，彻底处于“看风景”状态时，才悄悄恢复特效。

### 带来的收益

将单纯的“底层代码执行时序”问题，上升到了“交互认知”层面解决。利用魔术师般的注意力转移技巧掩盖了性能开销，从而在 DOM 节点极多的复杂应用中，实现了绝对无缝、如臂使指的 60fps 极限物理阻尼手感。在未来任何复杂的无限画布或高频动画项目中，此策略应作为**首选的性能架构标准**。

## 🏗️ 核心架构约束：数据层不得与 DOM 绑定 (Data-DOM Decoupling)

本项目未来可能从 DOM 渲染迁移到 Canvas、WebGL 或混合渲染架构。因此，从现在开始，业务数据、画布状态和业务逻辑必须独立于 React 组件及 DOM。

### 1. 数据模型是唯一事实来源
所有节点、连线、任务状态和生成结果必须保存为纯数据对象。节点的位置、尺寸、层级、连接关系和业务状态必须来自数据层，不能通过读取 DOM 得到。

### 2. 禁止把 DOM 当成数据存储
* 禁止使用 `element.dataset` 保存核心业务数据。
* 禁止使用 DOM 的位置作为节点真实位置。
* 禁止通过 `getBoundingClientRect()` 获取并保存节点世界坐标（仅限临时定位显示）。
* 禁止通过查询 DOM 判断节点是否存在、是否选中或是否正在运行。
* 禁止将 HTMLElement、React组件实例、ref、Event对象存入全局状态。
* 禁止直接修改 DOM 来改变业务状态。

### 3. React和DOM只是渲染层
正确的数据流：用户操作 → 调用业务命令 → 修改数据层 → React订阅数据变化 → DOM重新渲染。
禁止的数据流：用户操作 → 直接修改DOM → 再从DOM反推业务数据。

### 4. 业务逻辑不得依赖渲染技术
业务函数（如移动节点、连线、拆解剧本、生成任务等）只能接收普通数据，并返回普通数据或明确的状态更新。不得接收 DOM 节点、Event 对象或 React 实例。必须能够在没有浏览器DOM的环境中运行和测试。

### 5. 区分世界坐标与屏幕坐标
数据层只长期保存绝对的“世界坐标”。相机状态（x, y, zoom）单独保存。屏幕坐标只能由这两者临时计算得出。

### 6. 通过渲染适配层隔离技术实现
未来替换渲染器（DOM -> Canvas/WebGL）时，项目数据格式、Agent逻辑和生成任务逻辑必须保持零修改。

### 7. 数据必须可序列化
全局状态和项目文件中的数据必须能够安全执行 `JSON.stringify`。禁止在持久化数据中保存 DOM对象、函数、Promise 或 Canvas/WebGL 实例。

### 8. Agent修改代码前必须进行架构检查
每次新增或修改画布功能前，必须自我审查：状态是否纯数据？逻辑是否脱离DOM？数据能否序列化？换成PixiJS是否仍然可用？

### 9. 输出要求
每次涉及画布、节点或状态管理的代码修改后，必须在总结中说明：新增数据的位置、业务层与渲染层的划分、DOM依赖情况、未来迁移Canvas的替换范围、序列化能力。

## 🖼️ 资产卡片设计标准与无限画布性能架构 (Asset Card Architecture)
在处理图片和视频卡片时，必须严格遵守以下针对无限画布的极限性能架构。详细说明参见 `docs/architecture/asset_card_performance_spec.md`：
1. **视频卡片意图驱动懒挂载 (Lazy Mounting)**：平时必须表现为仅包含封面的纯 `div`，**绝对禁止**后台常驻 `<video>` 标签。只有当 `isHovered` 或 `isPlaying` 时才挂载 `<video>` 节点，离开即销毁，彻底释放 GPU 合成层负担。严禁使用 `backdrop-blur` 滤镜。
2. **图片卡片动态 LOD**：收敛至 Canvas 渲染，`< 200%` 缩放时仅渲染缩略图/2K 代理，仅在 `> 200%` 且处于视口中心且停止拖拽时，才通过透明度过渡渲染 4K 原图。
3. **视觉与数据深度解耦 (AI 数据流向)**：UI 永远渲染轻量代理图，但在进行 AI 生图/视频等业务请求时，必须无视 UI 状态，直接从底层模型 (`CardData`) 中提取最原始、未阉割的二进制对象 (`trueOriginalFileData` / `originalFileData`)，确保输入大模型的参考图 100% 无损。
